# Eval profile — configure judge ChatModel before running PromptEvalTest.
# Add a LangChain4j provider to eval/pom.xml (e.g., langchain4j-open-ai)
# then set credentials here or via environment variables.
#
# Example (OpenAI):
# quarkus.langchain4j.openai.api-key=${OPENAI_API_KEY}
# quarkus.langchain4j.openai.chat-model.temperature=0.0
