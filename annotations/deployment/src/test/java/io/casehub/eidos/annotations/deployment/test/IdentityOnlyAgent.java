package io.casehub.eidos.annotations.deployment.test;

import io.casehub.eidos.annotations.Identity;

@Identity(slot = "identity-only-agent", briefing = "No disposition, no goals, no constraints")
public interface IdentityOnlyAgent {}
